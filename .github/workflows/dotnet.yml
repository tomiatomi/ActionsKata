name: .NET

on: 
  pull_request:
    branches: main
              "ci_cd_seminar"

jobs:
  welcome:
    runs-on: ubuntu-latest
    
    steps:
    - name: Hello message
      run: echo "Hello guys"
      
    - name: build
    - uses: actions/checkout@v3
    - uses: actions/setup-dotnet@v2
      with:
        dotnet-version: "3.1.x"
    - run: dotnet build
